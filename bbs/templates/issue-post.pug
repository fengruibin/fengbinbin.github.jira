extends layout.pug 

block content 
  if isLogin 
    h2 发帖
    form(action="/post" method="POST") 
      div Title: 
      input(type="text" name="title")
      div Content: 
      textarea(name="content", cols="30", rows="10") 
      #button 
        button 发布帖子
  else 
    p 未登录，请#[a(href="login") 登录]发帖